# Preferences (L3)

Curated style, naming, formatting, and workflow choices. One bullet block per entry; see `SCHEMA.md`.

_Auto-managed by `agentic-kit/tools/memory-promote.sh`. Hand-edits are allowed but must follow the schema (entries with malformed `id:` will be re-hashed on next promote)._

<!-- entries below -->
