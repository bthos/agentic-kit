# Projects (L3)

Per-feature canonical summaries — the "what was actually built and why" record. One bullet block per entry; see `SCHEMA.md`.

_Auto-managed by `agentic-kit/memory/tools/promote.sh` from `.akt/archive/<feature>/LESSONS.md` and `handoff-log.md`._

<!-- entries below -->
