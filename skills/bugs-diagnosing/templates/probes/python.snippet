# YAGA probe — Python
# Investigation: {{INVESTIGATION_ID}}
# Port: replace <PORT> with the value from server.json. Inline as a literal.
# Strip: removed by talaka/shared/debug/tools/debug-strip.sh on cleanup.

import json as _dbg_json, urllib.request as _dbg_req  # DEBUG:{{INVESTIGATION_ID}}
try: _dbg_req.urlopen(_dbg_req.Request("http://127.0.0.1:<PORT>/log", data=_dbg_json.dumps({"id":"DEBUG:{{INVESTIGATION_ID}}","probe":"p1","file":__file__,"vars":{ }}).encode(), headers={"content-type":"application/json"}), timeout=0.2)  # DEBUG:{{INVESTIGATION_ID}}
except Exception: pass  # DEBUG:{{INVESTIGATION_ID}}
