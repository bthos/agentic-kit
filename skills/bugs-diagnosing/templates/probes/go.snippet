// YAGA probe — Go
// Investigation: {{INVESTIGATION_ID}}
// Port: replace <PORT> with the value from server.json. Inline as a literal.
// Strip: removed by talaka/shared/debug/tools/debug-strip.sh on cleanup.
// Requires: "bytes", "net/http" (likely already imported by the file).

go func() { _, _ = http.Post("http://127.0.0.1:<PORT>/log", "application/json", bytes.NewReader([]byte(`{"id":"DEBUG:{{INVESTIGATION_ID}}","probe":"p1","vars":{}}`))) }()  // DEBUG:{{INVESTIGATION_ID}}
